// VerdantiaListenersExtension — stub/scratch directory only.
// The live extension is VerdantListenersExtension.
// This directory holds WIP/flagged source files that are NOT built into a deployed jar.
// No typewriter {} block — Gradle will warn "No extension configuration found" and skip it,
// which is the correct behaviour for a non-shipping scratch module.
